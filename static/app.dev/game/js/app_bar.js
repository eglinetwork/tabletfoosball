/**
 * @author egli
 */

YUI.add('app_bar', function(Y) {
 
    // Private functions and vars


    Y.namespace('APP');
    Y.APP.bar = {
        init : function(bar,x){
            _init(bar,x);
        }
    }
}, '0.0.1' /* module version */, {
    requires: []
});

